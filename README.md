# Notas:

```
Options:
      --help     Show help                                             [boolean]
      --version  Show version number                                   [boolean]
  -b, --base     Número base para crear la tabla                        [number]
  -l, --listar   Mostrar tabla al crearla             [boolean] [default: false]
  -h, --hasta    Límite de múltiplicación                 [number] [default: 10]

```